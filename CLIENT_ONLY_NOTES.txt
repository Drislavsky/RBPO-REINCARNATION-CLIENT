CLIENT ONLY FIX

This archive contains only the Windows client/service project. The server project is not included and was not modified.

Changed client-side files:
- rpc/bmtx_rpc.idl
- src/service.cpp
- src/main.cpp
- CMakeLists.txt

Important:
- AV signatures are not hardcoded in the service.
- BMTXService loads AV signatures from the existing server endpoint /api/signatures after activation and before db-info/scan requests.
- Rebuild this project and reinstall BMTXService.exe.
